# AGENTS.md

The instructions for this repo live in [`CLAUDE.md`](CLAUDE.md). Read that.

This file used to be a byte-for-byte copy of it, which meant every rule
change had to be made twice and drifted the moment one was forgotten. It is
a pointer now so there is only ever one source of truth.
